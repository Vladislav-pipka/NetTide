// import 'package:google_mlkit_translation/google_mlkit_translation.dart';

class TranslationService {
  // final _modelManager = OnDeviceTranslatorModelManager();

  // Future<bool> isModelDownloaded(String languageCode) async {
  //   return await _modelManager.isModelDownloaded(languageCode);
  // }

  // Future<void> downloadModel(String languageCode) async {
  //   await _modelManager.downloadModel(languageCode);
  // }

  // Future<void> deleteModel(String languageCode) async {
  //   await _modelManager.deleteModel(languageCode);
  // }

  // OnDeviceTranslator getTranslator(TranslateLanguage source, TranslateLanguage target) {
  //   return OnDeviceTranslator(sourceLanguage: source, targetLanguage: target);
  // }

  // Future<String> translate(String text, String sourceLanguage, String targetLanguage) async {
  //   final translator = OnDeviceTranslator(
  //     sourceLanguage: TranslateLanguage.values.firstWhere((lang) => lang.bcpCode == sourceLanguage),
  //     targetLanguage: TranslateLanguage.values.firstWhere((lang) => lang.bcpCode == targetLanguage),
  //   );
  //   final translatedText = await translator.translateText(text);
  //   translator.close();
  //   return translatedText;
  // }
}
